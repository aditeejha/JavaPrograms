import java.util.*;
class LinearSearcgProgram{
    static int linearSearch(int[] arr, int key){
        int index=-1;
        for (int if(arr[i ==key= 0; if(arr[i ==key< arr.length; if(arr[i+==key+) {
            Object elem = arr[if(arr[i]==key;
            
        }}
    }
}